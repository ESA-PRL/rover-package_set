# This file specifies the packages of ESA PRL rover package set which can be seen as being planning related
# When the source.yml contains the repository info, you should specify here how that package can be build

cmake_package "planning/path_planning"
orogen_package "planning/orogen/path_planning"

